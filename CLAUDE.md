# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Commands

```bash
npm run dev              # Start frontend (Vite) + backend (Convex) in parallel
npm run dev:frontend     # Start only Vite dev server
npm run dev:backend      # Start only Convex dev server
npm run build            # Build frontend (vite build)
npm run convert-images   # Convert PNG images to WebP format
npm run lint             # Full check: tsc (convex + app) → convex dev --once → vite build
npx convex dev --once    # Push schema/functions to Convex without watching
```

Tests: `npm run test` (vitest + convex-test; `*.test.ts` em `convex/`). Seed data is available via `convex/seed.ts`.

## Architecture

Multi-tenant CRM with human-AI team collaboration. Convex backend, React + TailwindCSS frontend.

**Multi-tenancy:** Every table has `organizationId`. All queries must be scoped to the user's org.

**Auth flow:** `requireAuth(ctx, organizationId)` from `convex/lib/auth.ts` handles auth + org membership. Returns the team member. `requirePermission(ctx, organizationId, category, level)` extends this with granular RBAC checks. A few functions without org context (e.g. `getUserOrganizations`) still use `getAuthUserId` directly.

**Permissions (RBAC):** 9 permission categories (leads, contacts, inbox, tasks, reports, team, settings, auditLogs, apiKeys) with hierarchical levels. Defined in `convex/lib/permissions.ts`. Each role (admin, manager, agent, ai) has sensible defaults; admins can override per-member. Frontend uses `usePermissions(organizationId)` hook and `<PermissionGate>` component.

**Invite flow:** Admins invite humans via `inviteHumanMember` action (creates auth account with bcrypt-hashed temp password). New users must change password on first login (`mustChangePassword` flag). Users auto-linked to org via `afterUserCreatedOrUpdated` auth callback.

**Side effects in mutations:** Most mutations insert into `activities` + `auditLogs` and trigger webhooks via `ctx.scheduler.runAfter(0, internal.nodeActions.triggerWebhooks, ...)`.

**Email/Notifications:** `@convex-dev/resend` component for email delivery. Central dispatch via `convex/email.ts` — all events go through `internal.email.dispatchNotification`. Templates in `convex/emailTemplates.ts` (PT-BR). Per-member preferences in `notificationPreferences` table (opt-out model — no row means all enabled). Convex components registered in `convex/convex.config.ts`. **Email env vars:** `RESEND_API_KEY`, `APP_URL`, `RESEND_FROM_EMAIL` (default: `HNBCRM <noreply@mail.hnbcrm.com>`), `RESEND_WEBHOOK_SECRET`. Domain: `mail.hnbcrm.com` (subdomain to avoid Gmail MX conflict).

**HTTP API:** `convex/router.ts` has RESTful endpoints at `/api/v1/` authenticated via `X-API-Key` header. API keys resolve permissions from key → team member → role defaults. **RBAC enforçado em TODAS as rotas (v0.47):** mapa único `ROUTE_PERMISSIONS` (`"MÉTODO /rota"` → categoria+nível espelhando o `requirePermission` da função app equivalente, ou `"authenticated"` p/ rotas self-scoped) + `requireRoutePermission` logo após `authenticateApiKey` em cada handler; fail-closed (rota sem entrada = 403 "Permissão insuficiente"); teste de build `routerPermissions.test.ts` garante completude e gate em cada handler. Rate limit por key: 300 req/min (fixed window em `apiKeys.updateLastUsed`, 429). Routes wired in `convex/http.ts`.

**WhatsApp channel:** Per-organization channel configs (`convex/channelConfigs.ts`, `channelConfigs` table) with `provider: "meta" | "bridge"` — two transports on the same `whatsapp` channel. `meta` = official WhatsApp Cloud API (Graph API, 24h service window, message templates); ingress at `GET/POST /webhooks/whatsapp` (`convex/whatsapp.ts`). `bridge` = self-hosted wuzapi gateway (WhatsApp Web protocol via whatsmeow, QR pairing, no 24h window, no templates); ingress at `POST /webhooks/bridge` verified by HMAC-SHA256 via env `WA_BRIDGE_HMAC_SECRET` (`convex/bridge.ts`). Managed provisioning: envs `WA_BRIDGE_DEFAULT_URL` + `WA_BRIDGE_ADMIN_TOKEN` (both set = "Servidor HNBCRM" default in the provisioning form — user only types a display name; admin token never leaves the server; advanced users can still point to their own gateway). Bridge is opt-in per org and carries a permanent-ban risk (unofficial protocol, violates WhatsApp ToS). Key files: `convex/bridge.ts`, `convex/lib/bridgeParse.ts` (pure parser), `convex/lib/bridgeSend.ts` (text), `convex/lib/bridgeMedia.ts` (media), `convex/lib/bridgeSession.ts` (QR/status/provisioning). Outbound dispatch in `convex/whatsapp.ts` branches by provider (voice notes are transcoded to ogg/opus via the Whisper service's `/convert`). Typing presence and delivery/read receipts flow both ways (`sendTypingState`, `markConversationRead`, `ChatPresence`/`ReadReceipt` webhook events). UI in `src/components/settings/ChannelsSection.tsx` (+ `ChannelHealthPanel` with 7-day delivery stats).

**Inbox features:** Voice-note transcription via self-hosted Whisper (`convex/transcription.ts`, env `WHISPER_SERVICE_URL`/`WHISPER_SERVICE_TOKEN`, opt-in per org via `channelConfigs.autoTranscribeAudio`; transcript mirrored to `messages.transcriptText`). Full-text message search (search indexes `search_content` + `search_transcript` on `messages`, query `conversations.searchMessages`). Quick replies (`convex/quickReplies.ts`, "/" trigger in the composer). Scheduled messages (`convex/scheduledMessages.ts`, `ctx.scheduler.runAt`). Conversation archiving + org-scoped labels (`conversations.ts`, `conversationLabels` table) with bulk actions. Inbox UI pieces live in `src/components/inbox/`.

**Tasks (P1):** Full task manager beyond the original lead/contact-linked reminders. Projects/lists (`taskProjects`) each with kanban columns (`taskColumns` — done column, WIP limit informative only) and manual card ordering; org-wide labels with color (`taskLabels`); multiple assignees per task (`assigneeIds`, humans and AI — `assignedTo` always mirrors the first one for backward compatibility); real subtasks (`parentTaskId` hierarchy, with aggregated progress) plus informative dependencies (`blockedBy`, not enforced server-side); recurrence lineage moved to its own field (`recurrenceSourceId`) so it no longer collides with subtasks; per-task early reminder (`reminderMinutesBefore`) in addition to the due-date reminder; `@mentions` in task comments that actually notify; in-app notifications (`notifications` table, bell in the header) alongside existing e-mail notifications; server-side full-text search and saved filters (`savedViews` for `entityType: "tasks"`) wired into the UI. **Task↔lead linkage (v0.44):** lead chip on task rows/cards, "Lead" section in the task detail (link/change/unlink via `updateTask` — `leadId`/`contactId` accept `null` to clear, new links validated against the task's org — plus jump to the lead's conversation), Lead field in CreateTaskModal. **Deep-links:** `/app/tarefas?task=<id>` (task detail), `/app/pipeline?lead=<id>` (board switches to the lead's board and opens LeadDetailPanel; panel open/close syncs the param so browser back works), `/app/entrada?conversation=<id>` (inbox selects the conversation; only matches the loaded non-archived list). Lead panel's Tarefas tab navigates to the task deep-link. New webhook events: `task.moved`, `task.due_soon`, `task_project.*`, `task_label.*`. The REST API (`/api/v1/tasks/*`) and MCP tools were NOT extended with write params for the new fields (project/labels/assignees still create/update only through the app UI) — GET responses do include the new raw document fields. Key files: `convex/taskProjects.ts`, `convex/taskLabels.ts`, `convex/notifications.ts`, `convex/lib/notify.ts`, `src/components/tasks/`, `src/components/notifications/`.

**Devolver à IA respondendo o que ela precisava (v0.50):** o fluxo "gerente responde ao atendente" — humano informa um dado (Pix, valor, data) e a IA continua o atendimento sabendo dele. (1) **Notas da equipe persistentes:** `conversations.aiTeamNotes` (cap 20, FIFO) — toda instrução de `returnToAi`/reject-com-instrução é gravada como nota e entra no prompt de TODOS os turnos seguintes da conversa como "INFORMAÇÕES DA SUA EQUIPE" (fonte oficial confirmada; a nota igual à instrução do turno atual é filtrada para não duplicar). (2) **Prompt reforçado:** os blocos de instrução/notas agora vencem explicitamente regras de PERSONA do tipo "você não sabe/não tem acesso/quem confirma é a equipe" (sem isso a IA recusava a informação que a equipe acabou de passar — caso real do Guardião) e proíbem `requestHandoff` para o que a instrução já resolve; só as REGRAS OBRIGATÓRIAS da plataforma seguem acima. (3) **`handoffs.rejectHandoff` aceita `instruction`:** rejeita + agenda `internal.attendant.internalQueueInstructedTurn` (via scheduler — import direto criaria ciclo handoffs↔attendant), que faz a devolução plena (despausa + lead de volta ao atendente) e dispara o turno instruído; a instrução vira `notes` do handoff. UI: popover de instrução (`AiInstructionPopover`, agora exportado de `AiDraftCard.tsx`, com `direction="up"`) no botão "Devolver à IA" do card em /app/repasses e no rodapé do `HandoffPeekSlideOver`. (4) **Correções:** núcleo único `queueInstructedAiTurn` em `attendant.ts` (item coach pendente convertido limpa `sourceDraftId`); `internalCommitAiReply` ganhou o mesmo bypass humano do claim (arg `humanInitiated`) e marca rascunhos `pending` antigos como `revised` ao enviar direto (sem órfãos no inbox); `returnToAi` sem mensagens não perde mais a instrução (nota persiste). Testes em `attendantCoach.test.ts` ("notas da equipe e devolver com instrução").

**Atendente IA "vê" imagens (v0.51, opt-in, default OFF):** o cliente manda o print do comprovante de Pix e a IA responde ao que está nele, em vez de responder no escuro. **Regra central: a imagem vai ao provider UMA VEZ POR IMAGEM, para sempre** — `internal.vision.autoDescribe` (agendado pelo ingest em `conversations.internalReceiveMessage`) faz 1 chamada a um LLM com visão e grava o resultado como TEXTO (`messages.imageDescription` + `messages.metadata.vision = {status,text,tipo,fields,model,provider,error,at}`); o turno do atendente continua 100% texto no `deepseek-v4-flash`, lendo `[imagem descrita]: …` do histórico. Uma conversa de 40 mensagens com 1 comprovante custa 1 chamada de visão, não 40. **Gate — UM interruptor só (v0.52):** `orgAiActive && aiConfig.visionEnabled === true`. `visionEnabled` ausente = DESLIGADO (diferente de `copilotEnabled`/`attendantEnabled`, onde `undefined` = ligado); com ele off, zero chamada e o histórico volta ao `[imagem]` cru. A v0.51 exigia também `channelConfigs.autoDescribeImages`, num AND — dois interruptores em telas diferentes para ligar a mesma coisa; o campo virou LEGADO no schema (nada lê nem escreve; fica porque o Convex valida os documentos existentes no push). **Allowlist explícita de modelo POR ROTA** em `lib/llm/registry.ts` (`VISION_MODELS_BY_PROVIDER`, `supportsVision`/`visionChainFor` fail-closed): opencode-go = `deepseek-v4-flash-vision-exp` → `glm-5.3-flash` → `kimi-k3`; openrouter (ZDR travado) = `glm-5.3-flash` → `kimi-k2.7-code` → `mimo-v2.5`. Nada de "tenta e vê": `hy3` e `longcat-2.0` aceitam a imagem, ignoram em silêncio e devolvem tudo `null`. É por rota porque sob o body ZDR do OpenRouter o melhor modelo devolve 404 ("No endpoints found matching your data policy"). `vision.ts` tem loop de fallover PRÓPRIO (qualquer erro = próximo elo; `shouldFallover` propagaria 400 genuíno) + timeout de 45 s + `max_tokens` 1500 (reasoning models zeram com 500) + parse tolerante a cerca ```json e `<think>…</think>`. A imagem sobe como **data URI base64 montada dentro da action** (a URL do Convex Storage é pública e sem expiração), sem resize, com teto de 5 MB; figurinha nunca é descrita. **Espera generalizada:** `hasMediaAwaitingEnrichment` (ex-`hasAudioAwaitingTranscription`) cobre transcrição pendente, visão pendente e download em voo (`metadata.mediaPending`), com `aiReplyQueue.mediaWaitUntil` (o `transcriptWaitUntil` fica como legado, sem migração). **Anti-injeção em 2 camadas:** system prompt defensivo no passe de visão + REGRA 7 no prompt do atendente (a descrição TRANSCREVE o texto da imagem, então o payload entra no histórico — ele viaja dentro do envelope não-confiável). **D13 — a IA lê mas NÃO confirma pagamento:** diz o que leu ("recebi seu comprovante de R$ X de DD/MM") e deixa a baixa para a equipe. Runs em `agentRuns` com `kind: "vision"`. UI: `ImageDescription.tsx` no inbox (espelha `VoiceTranscription.tsx`, com os campos do comprovante), CTA manual "Ler imagem" (`vision.describeImage`, gate `inbox:view_own` + toggle mestre), o interruptor em Configurações → IA → "Produtos de IA", e busca pelo índice `search_image`. **LGPD:** a imagem do cliente (que pode ser RG/CNH) sai para provider externo — daí o opt-in explícito; rever a cláusula do `lgpdAck` antes de ligar em produção. Custo real medido (rota openrouter/glm-5.3-flash): ~US$ 0,0002–0,0004 por imagem — o `/v1/models` do OpenCode Go não publica preço, então o do `deepseek-v4-flash-vision-exp` segue desconhecido. Fora de escopo: PDF (os modelos não aceitam PDF como `image_url`) e simulador/evals com imagem. Testes: `convex/attendantVision.test.ts`.

**Defesas da mídia inbound (v0.53):** três achados colaterais do plano de visão, corrigidos. (1) **Allowlist + quota na porta de entrada:** `internalSaveInboundAttachment` (`convex/whatsapp.ts`, usada pelos DOIS transportes) passou a aplicar as mesmas defesas do upload humano — `checkInboundMediaMimeType` (allowlist do upload + extras que só chegam pelo WhatsApp: vídeo, codecs de celular, zip, slides) e `checkInboundMediaQuota`. Diferença deliberada: **nada lança** — quem manda o anexo é o contato, e derrubar a mensagem dele perderia o texto no inbox; recusa devolve `{ ok:false, reason }`, o ingest marca `metadata.mediaSkipped` e o blob já armazenado é apagado na hora (não há cron de limpeza de órfão). O teto por arquivo do plano NÃO se aplica — quem limita é o `MAX_MEDIA_BYTES` de 25 MB do ingest. (2) **`MediaKey` fora do banco:** `metadata.bridgeMedia` guardava o descriptor whatsmeow inteiro, com a chave que decifra o blob na CDN, para sempre. Agora só `sanitizeBridgeMediaMeta` (kind/mimeType/filename/fileLength) é persistido, e `stripMediaKeyMaterial` varre o metadata inteiro antes do banco como rede de segurança (pega até o `metadata.raw` de tipo desconhecido). Backfill: `internal.bridge.internalBackfillBridgeMediaKeys`. (3) **Blob compartilhado:** `forwardMessage` duplica a linha de `files` com o mesmo `storageId` (de propósito — `files.messageId` é 1:1), e qualquer exclusão apagava o blob deixando a outra cópia quebrada. `lib/fileRefs.ts` condiciona só o `ctx.storage.delete` a não haver outra linha com aquele `storageId` (índice `by_storage_id`); na dúvida o blob FICA. Usado por `files.deleteFile`, `files.internalDeleteFile` e a cascata de lead. Testes: `convex/inboundMediaSecurity.test.ts`.

**Correção da espera de mídia (v0.53):** `hasMediaAwaitingEnrichment` deixou de esperar por `metadata.mediaPending`. O nome engana: o download do bridge é SÍNCRONO dentro da action de ingest e a mensagem só é gravada depois dele, então o campo nunca significa "os bytes estão a caminho" — só é escrito em caminhos de FALHA (mídia grande demais, erro de download, anexo recusado), o Meta nem o escreve, e o inbox o lê como `hasMediaProblem`. A v0.51 esperava por ele e atrasava em 60 s toda resposta a uma mídia que falhou.

**Mídia que a IA não consegue ler (v0.53):** o PDF continua fora do alcance da visão (nenhum modelo da cadeia aceita PDF como `image_url`, e não há como rasterizar numa action do Convex), mas mandar comprovante em PDF é comportamento comum de cliente — e até aqui a IA via só `"comprovante-pix.pdf"` no histórico e respondia como se fosse uma mensagem de texto. `historyTextOf` ganhou o ramo de `contentType: "file"`: `[arquivo recebido: <nome> — não consigo abrir arquivos]`, `[arquivo recebido — não consigo abrir arquivos]` quando o parser só tem placeholder, `[vídeo recebido — não consigo assistir a vídeos]` (mesmo contentType, mídia diferente) e `[arquivo enviado]` no outbound. **Não é gated por `visionEnabled`** — a IA nunca conseguiu abrir PDF, com ou sem visão. **REGRA 8** no prompt manda dizer que não abriu, confirmar pelo nome e pedir um print (que a visão lê), e reitera o D13 para o caso de o nome parecer comprovante.

**Simulador com mídia (v0.53):** `simulateAttendant` aceita `image?`/`file?` no transcript, ao lado do `audio?` que já existia (imagem: o texto é a DESCRIÇÃO que a visão produziria; arquivo: o texto é o NOME). O botão "Testar" da config do atendente tem 3 alternadores no compositor, então dá para validar a persona contra um comprovante ou um PDF sem mandar do celular. **Goldens curadas** em `agentEvals` passaram de 1 para 3 (`CURATED_GOLDENS`, instaladas por `internalSeedCuratedGoldens` — substitui `internalSeedAudioGolden`): a de áudio, uma de **comprovante por imagem protegendo o D13** (lê e cita valor/data, mas não confirma pagamento — a regra mais fácil de quebrar sem ninguém notar ao reescrever a persona) e uma de **PDF** (não finge ter lido, pede print). `agentEvals.transcript` ganhou `image`/`file` no schema.

**Config de IA por produto (v0.52):** cada produto de IA (`copilot` | `attendant` | `vision`) escolhe a PRÓPRIA rota, e a visão também o próprio modelo — em `organizations.settings.aiConfig.providerConfig.products` (`{ order?, model? }` por produto; ausente = herda). Precedência da rota, do mais específico ao mais geral: **produto > padrão da org (`platformOrder`) > env `LLM_PLATFORM_ORDER` > `auto`**, resolvida por `effectivePlatformOrder` em `lib/agentRoutes.ts`; `resolveOrgRoutes` ganhou o 5º arg `product`. **A chave própria (BYO) continua sendo da ORGANIZAÇÃO inteira** — ela não tem fallback, e deixar um produto sozinho numa rota caída é falha silenciosa. **Modelo de visão:** `products.vision.model` ausente = "Automático" (a cadeia inteira com fallover); fixado = cadeia de um elo só, restrito à allowlist medida (`visionModelOptions()` no registry, com latência/tokens/acurácia de cada um) e validado no servidor por `setProductRouting` — um id fora da lista é RECUSADO na hora, porque em runtime ele viraria uma cadeia vazia e a org só descobriria quando a primeira imagem não fosse lida. Mutation `aiSettings.setProductRouting`, query `aiSettings.getVisionModelOptions`, e `getAiStatus` devolve `products` com os sentinelas `order: "inherit"` / `model: ""`. **UI reorganizada:** "Produtos de IA" passou a carregar interruptor + modelo + rota de cada produto (antes o interruptor ficava num card e o modelo noutro, e a visão ainda pedia um terceiro toggle em Canais); o card de baixo virou "Provider e privacidade" e guarda só o que é da org — rota padrão, chave própria (BYO), modo estrito ZDR e "Testar conexão".

**Handoffs fluidos + coaching (v0.45):** todos os gatilhos de repasse (humano, palavra-chave, tool da IA, falha) passam por `createHandoffCore` em `convex/handoffs.ts` — resolve a `conversationId` de origem (fallback: conversa mais recente não arquivada do lead), notifica in-app (destinatário definido → só ele; sem destinatário → broadcast p/ quem tem `inbox >= reply`) e dispara webhook. Criar repasse NÃO pausa a conversa (a elegibilidade da IA já segura enquanto pendente); ACEITAR assume (pausa IA + atribui lead + desarquiva) e a UI navega para `/app/entrada?conversation=<id>`; REJEITAR devolve à IA. UI: `/app/repasses` com deep-link `?handoff=<id>` e "Espiar conversa" (`src/components/handoffs/HandoffPeekSlideOver.tsx`, read-only com resumo/BANT) + banner âmbar na conversa com repasse pendente (aceitar inline). **Loop de coaching** (SÓ app UI, não exposto em REST/MCP): no rascunho em modo sugestão o humano instrui em linguagem natural (chips + campo livre) e regenera — o rascunho antigo vira `revised` (encadeado por `previousDraftId`/`nextDraftId`, fora das métricas do gate do autopilot); `attendant.requestAiDraft` gera rascunho do zero com instrução opcional; `attendant.returnToAi` despausa + reatribui ao atendente + cancela repasse pendente. Turno pedido por humano SEMPRE gera sugestão, mesmo em org autopilot (`aiReplyQueue.origin` = `coach` | `return_to_ai`, run marcada `humanInitiated`). Schema: `handoffs.conversationId` + status `canceled`; `notifications` ganhou `handoff_requested`/`handoff_resolved`/`ai_draft_pending` + ponteiros `handoffId`/`conversationId`; preferência `aiDraftPending`; métricas do atendente com contadores `revised`/`coached` fora do gate. Webhooks novos: `handoff.canceled`, `conversation.returned_to_ai` (`handoff.requested` agora carrega `conversationId` + `origin`; `handoff.accepted` carrega `conversationId`). REST inalterado exceto `GET /api/v1/handoffs`, que devolve `conversationId` e aceita `status=canceled`.

**Arquivamento/exclusão de pipelines e leads (v0.48):** `boards.archivedAt` (soft-delete, mesmo padrão de leads/conversations/taskProjects) + `boards.deletionStartedAt` (board em exclusão diferida — `unarchiveBoard`/`deleteBoard` recusam re-entrada; UI mostra "Excluindo…"). `archiveBoard`/`unarchiveBoard`/`deleteBoard({ deleteLeads?, deleteContacts? })` em `convex/boards.ts`: bloqueiam o último pipeline ATIVO, promovem `isDefault` ao board ativo de menor `order`, e board com leads só sai com `deleteLeads:true` (senão o erro antigo). Todos os fallbacks de "board ativo" (router, attendant, formSubmissions, inboundRouting, imports, copilot, dashboard, `internalGetBoards`) excluem arquivados; exports/seed deliberadamente NÃO filtram. **Exclusão de lead:** núcleo único `hardDeleteLead` em `convex/lib/leadCascade.ts` usado por `deleteLead({ deleteContact? })`, `bulkDeleteLeads` (teto 100), REST `internalDeleteLead` e pelo copiloto — audit log com **snapshot completo achatado em `changes.before`** (consultável em /app/auditoria; `_id`→`id` pois Convex rejeita campo com underscore), webhook `lead.deleted`, e cascata batched (~150 escritas/execução, jobs `internalCascadeDeleteLeads`/`internalDeleteBoardCascade` que se re-agendam — um job sequencial, nunca N por lead): apaga conversations+messages (+ blobs de mídia), handoffs, activities, leadDocuments, aiReplyQueue/scheduledMessages da conversa; limpa refs opcionais de tasks/calendarEvents/files; preserva formSubmissions/agentRuns. `deleteContact` só apaga contato sem NENHUM outro lead (com snapshot no audit). Queries de impacto p/ dialogs: `getBoardDeletionImpact`, `getLeadDeletionImpact` (gate `leads:full`, igual ao delete). UI: `DeleteBoardModal.tsx` (digitar o nome + checkbox de contatos default OFF), pill "Arquivados (N)" no seletor de boards, menu "⋮" no `LeadDetailPanel` (arquivar/restaurar/excluir com impacto), bulk delete na `LeadsBulkActionBar` (gate `leads:full`). Testes: `boardsArchiveDelete.test.ts`, `leadDelete.test.ts` (drenar jobs em teste: `vi.useFakeTimers()` + `t.finishAllScheduledFunctions(vi.runAllTimers)`; NÃO usar `finishInProgressScheduledFunctions` intercalado com `t.run`). Ponteiros pendurados conhecidos (aceitos): `notifications.conversationId/handoffId` não são limpos; `leads.getLeads` sem `boardId` ainda lista leads de board arquivado.

**Painel de lead sobreposto (v0.48):** `LeadDetailPanel` ganhou `initialTab` e é montado como slide-over SOBRE outras telas sem navegar: no Inbox ("Ver lead" no header abre com aba Detalhes; ícone ExternalLink preserva "Abrir no funil"; nome do contato no header abre `ContactDetailPanel` sobreposto) e na aba "Leads Vinculados" do `ContactDetailPanel` (cards clicáveis com cor do estágio, board, responsável e data; arquivados em seção colapsada; o painel de lead é renderizado como irmão DEPOIS do SlideOver p/ ficar por cima — ambos `z-50`, ordem no DOM decide). `getContactWithLeads` devolve todos os leads (incl. arquivados) enriquecidos com `stage {name,color}`/`boardName`/`assigneeName`.

**Export/Import de dados (v0.46):** aba "Dados" em Configurações (`src/components/settings/DataSection.tsx`, deep-link `/app/configuracoes?secao=data`) — tudo atrás de `settings:manage` (default: só admin) e auditado a cada transição de job (backup completo = severity `high`). **Export** (`convex/exports.ts` + `lib/exportColumns.ts`): CSV por entidade (contatos/leads/tarefas, colunas desnormalizadas + `cf_<key>`, BOM + RFC 4180) e backup JSON versionado (`hnbcrm-backup` v1, 22 tabelas core na ordem da seção 7 do plano, sanitizado por `lib/exportSanitize.ts` — teste de build `exportSecurity.test.ts` quebra se segredo vazar); resultado vai para o File Storage (fora da quota de `files`), expira em 7 dias (cron horário `internalCleanupExpired`); paginação de 500 via `lib/cursor.ts`; 1 job ativo por org. **Import** (`convex/imports.ts` + `convex/importRun.ts`): wizard server-side de contatos/leads — upload padrão de `files` com `fileType:"import_file"` → detecção de headers + sugestão de mapeamento (`lib/importMapping.ts`: aliases PT-BR/EN, coerções data/número/booleano/tags, custom fields `cf:<key>` validados contra `fieldDefinitions`) → dry-run com caps (50 erros de amostra, 10 preview, teto 10k linhas) → execução em lotes de 50 com trilha de rollback em `importJobBatches` (`createdIds` + `before` só dos campos alterados; `null` = campo não existia) → rollback deleta criados e reverte atualizados; leads resolvem board/estágio por nome (fallback board default), find-or-create de contato por email/telefone, estratégias `skip`/`update`/`create` (update nunca apaga, tags somam). **Pegadinha:** chaves dos records `mapping`/`suggestedMapping` são `encodeHeaderKey(header)` (= encodeURIComponent; Convex rejeita acento em nome de campo) — helper compartilhado em `convex/lib/importKeys.ts` (front e REST usam o mesmo); `getFailedRowsCsv` é ACTION (lê storage). CSV: `convex/lib/csv.ts` é a referência única (parse RFC 4180 + auto-detect `,`/`;` + serialize com BOM). UI: `ImportWizard.tsx` (5 passos, retoma pelo status do job) + `FileDropZone.tsx`. REST: 12 rotas `/api/v1/exports/*` e `/api/v1/imports/*` (desde a v0.47 TODAS as rotas da API enforçam permissão — ver abaixo); download com `Content-Disposition: attachment`; import inline ≤ 5 MB ou via `fileId`. CSVs de export escapam fórmulas (`'` antes de `=`/`+`/`@`; números negativos preservados) e o import desfaz o prefixo no round-trip. Webhooks: `export.completed/failed`, `import.completed/failed/rolled_back`. **Exceção DELIBERADA ao checklist de side effects:** import/rollback NÃO gravam activity/audit/webhook por linha (contato/lead) — só por transição de job (decisão 9 da seção 2 do plano; 10k linhas = 10k schedulers); a trilha por registro fica em `importJobBatches`. FORA de escopo v1: restore do backup JSON, tools MCP, XLSX, export com filtros das list views.

**AI Agent Config (opt-in total):** dois produtos de IA num runtime provider-agnostic (OpenAI-compatible, default OpenCode Go — env `OPENCODE_GO_API`; fallback OpenRouter implementado, inativo sem `OPENROUTER_API_KEY`). (1) **Copiloto** in-app: chat SSE autenticado (`convex/copilotHttp.ts`, rota `/api/copilot/stream`), age COMO o usuário (actorType "human" + metadata.via:"copilot", RBAC dele via `assertAgentCan`), tools de leitura/escrita em `convex/copilot.ts`, destrutivo via `pendingActions` (two-phase, confirmação humana). (2) **Atendente** WhatsApp (`convex/attendant.ts`): gatilho de ingest ENFILEIRA em `aiReplyQueue` (debounce+coalescing), pacing por-org (`aiPacing`), lock OCC por conversa (`conversations.aiTurnLock`), envio só via `internalCommitAiReply` transacional que re-checa elegibilidade (TOCTOU); modo `suggest` default (rascunho revisado no inbox — `AiDraftCard`), `autopilot` só após métricas de aceitação (gate server-side); canais Meta sempre, bridge SOMENTE com aceite de risco org-level (`aiConfig.bridgeAiAck` — condição de elegibilidade re-checada no commit). Toggles separados `copilotEnabled`/`attendantEnabled` sob o mestre. **Pacing de envio (v4.1):** cursor em dois níveis em `lib/whatsappDispatch.ts` — por conversa (6,5s pair rate) + por número (`channelPacing`; Meta 1-3s, bridge reativo 4-10s, bridge frio 8-15s com jitter), typing humanizado no bridge p/ envios de IA/agendados, retry com backoff oficial 4^X (131056/130429/80007) e congelamento de canal em 131048; helper único de resolução de config em `lib/channelResolve.ts`. **Pipeline do atendente (P4):** `agentProfile.pipelineConfig` (board/estágio inicial no roteamento inbound, avanço determinístico pós-qualificação BANT, `allowMoveStages` com enforcement no executor, seção "REGRAS DO FUNIL" no prompt). Segurança: 4 camadas em `convex/lib/agentSecurity.ts` (assertAgentCan, escopo por registro, `TOOL_DENYLIST` + teste de build `agentToolSecurity.test.ts`, envelope de dado não-confiável `lib/promptEnvelope.ts`). Registry estático de tools: `convex/lib/agentTools.ts`. Camada LLM pura: `convex/lib/llm/` (adapter openaiCompatible, registry ZDR/modelos, fallback chain, sanitize). Config por-org em `organizations.settings.aiConfig` — `enabled` default FALSE + aceite LGPD obrigatório (`orgAiActive`); UI em Configurações → IA (`AiSection.tsx`). Runs auditadas em `agentRuns` (tokens/custo/tools — sem transcrições/PII). BYO key: `orgSecrets` (cifrada) + `lib/agentRoutes.ts`. **Roteamento de provider (por org + ops):** `providerConfig.platformOrder` ("auto" | "openrouter-first" | "*-only") reordena/filtra a cadeia da plataforma; UI em Configurações → IA (card "Provider, modelos e privacidade": roteamento, BYO key e botão "Testar conexão" → `aiDiagnostics.testOrgConnection`); override de ops via env `LLM_PLATFORM_ORDER` (vale p/ orgs sem escolha própria). Diagnóstico: `npx convex run aiDiagnostics:pingProvider '{}'` (cadeia com fallback) e `aiDiagnostics:pingChain '{}'` (cada hop isolado, expõe o erro real). Guard anti-vazamento de segredos: `convex/secretScan.test.ts` (quebra o build se um token real-parecido for commitado).

**Markdown na saída do Copiloto (v0.54):** o chat do copiloto imprimia `content` como texto cru (`whitespace-pre-wrap`), então `**negrito**`, tabela GFM e lista chegavam à tela com os asteriscos e os pipes — a resposta mais útil (a tabela de leads sem responsável) era justamente a mais ilegível. Renderer próprio, sem dependência nova (remark+gfm custaria mais em bundle que o recurso inteiro): `src/lib/markdown.ts` faz o parse para uma AST tipada e `src/components/ui/Markdown.tsx` monta elementos React a partir dela — **nunca HTML cru**, então tag vinda do LLM aparece como texto e `href` fora de `http/https/mailto/tel/relativo` é descartado. Cobre título, negrito/itálico/riscado, código inline e em cerca (com botão de copiar), lista (aninhada, ordenada com `start`), tabela GFM (rola na horizontal em vez de espremer o painel), citação, regra horizontal e link (inclusive URL solta). Dois desvios DELIBERADOS do CommonMark, porque a saída é chat: quebra de linha simples vira `<br>`, e `_` só delimita itálico em fronteira de palavra (senão `lead_id_final` viraria itálico). Tolerante a texto incompleto — em streaming a cerca ainda não fechou e a tabela pode ter só o cabeçalho. UI do `CopilotPanel`: a resposta da IA passou a ocupar a largura toda (a bolha de 85% espremia tabela e código), o painel abre mais largo em tela grande (`lg:600px`/`xl:680px`), cada resposta tem "copiar" no hover e o streaming mostra cursor. `buildSystemPrompt` (`convex/copilotHttp.ts`) ganhou a seção FORMATO: tabela só com até 4 colunas e células curtas, sem título de nível 1/2. Rascunho do Atendente segue texto puro — é mensagem de WhatsApp, não tem onde renderizar. Testes: `src/lib/markdown.test.ts` (o vitest passou a incluir `src/**/*.test.ts`).

**Frontend:** SPA with react-router v7. Dark theme default, mobile-first, PT-BR UI. `src/main.tsx` → `HelmetProvider` → `ConvexAuthProvider` → `RouterProvider`. Public routes: `/` (LandingPage), `/entrar` (AuthPage). App routes: `/app/*` wrapped by `AuthLayout` → `ScrollRestoration` → `AppShell` → `<Outlet />`. Page components get `organizationId` via `useOutletContext`. Reusable UI in `src/components/ui/`, layout in `src/components/layout/`. State is Convex reactive queries + local `useState`. Path alias `@/` → `src/`.

**Build optimizations:** Vite configured with manual chunking (react-vendor, convex-vendor, utils-vendor, icons-vendor), lazy loading for authenticated routes via React.lazy(), gzip/brotli compression, and bundle visualization (rollup-plugin-visualizer). Initial bundle: ~157 KB brotli (77% reduction from 1 MB baseline).

**SEO:** react-helmet-async for dynamic meta tags, Open Graph + Twitter Cards, JSON-LD structured data, sitemap.xml, robots.txt. Reusable `<SEO />` component in `src/components/SEO.tsx`.

## Convex Rules (mandatory)

Rules from `.cursor/rules/convex_rules.mdc` and `.claude/convex-agent-plugins/rules/`:

- **Always** use new function syntax: `query({ args: {}, returns: v.null(), handler: async (ctx, args) => {} })`
- **Always** include `args` and `returns` validators on every function
- **Never** use `.filter()` on queries — use `.withIndex()` instead
- **Never** use `Date.now()` inside queries — breaks reactivity
- **Never** schedule `api.*` functions — only schedule `internal.*` functions
- Use `internalQuery`/`internalMutation`/`internalAction` for non-public functions
- Actions using Node.js APIs must include `"use node";` directive and be in a separate file
- Use `ctx.db.patch` for partial updates, `ctx.db.replace` for full replacement
- Index naming: `by_<field>` or `by_<field1>_and_<field2>`
- Use `"skip"` pattern: pass `"skip"` to `useQuery` when args aren't ready
- Keep query/mutation handlers thin — extract business logic into plain TypeScript functions
- Use cursor-based pagination (`paginationOptsValidator`) for large datasets
